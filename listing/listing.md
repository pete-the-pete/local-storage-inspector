# Chrome Web Store Listing — Local Storage Inspector

Copy and assets for the Chrome Web Store listing. Keep it honest, keep it short, keep it scannable.

- **Version:** 1.5.1
- **Live listing:** https://chromewebstore.google.com/detail/local-storage-inspector/hjbhaeeadokbiijbekmfldgpddafeblb
- **Category:** Developer Tools
- **Language:** English

---

## Name

Local Storage Inspector

## Short description

Inspect and edit localStorage and sessionStorage in a side panel — with a real JSON editor and a live change log.

*(Chrome Web Store limit: 132 characters. Current: 129.)*

---

## Detailed description

Browser DevTools show you localStorage. They don't let you work with it.

**Local Storage Inspector** opens a side panel next to the page you're debugging and gives you the tools the Application tab has been missing:

### A real JSON editor
- CodeMirror 6 with One Dark syntax highlighting
- Inline validation — the Save button stays disabled until your JSON parses
- Pretty-print on open, compact on save
- Pretty-printed JSON is detected automatically; plain strings stay plain

### A live change log
- Every `setItem`, `removeItem`, and `clear` on the page is captured in real time
- Changes are attributed by source: `page` vs. `extension`, so you can see whether your app or you triggered it
- Pause and resume recording with one click
- Timestamps down to the millisecond

### Field-level diffs
- Collapsed rows show a one-line summary (`(new)`, `value changed`, or a list of changed JSON fields)
- Expand a row to see an **inline diff** with changed lines highlighted
- Toggle to a **unified diff** for the traditional `+`/`-` view

### Search, import, export
- Fuzzy-filter keys as you type
- Bulk import and export the current origin's storage as JSON
- Toggle between Local and Session storage with one click

### Layout that stays out of the way
- Drag handles resize the key list and change log panels
- Collapse the key list when you need more editor room
- Re-open the panel and it remembers your layout

### Privacy
- No network calls. No analytics. No telemetry.
- Storage data stays on your machine — it's read from and written to the page you're inspecting, nothing more.
- Host permissions are optional and requested per-site on demand.

### Built for engineers
- Manifest V3
- Open source (MIT) — https://github.com/pete-the-pete/local-storage-inspector
- TypeScript, React 19, CodeMirror 6
- Covered by unit and end-to-end tests

Click the extension icon on any page and the side panel opens. That's the whole onboarding.

---

## Screenshots

Chrome Web Store accepts up to five screenshots at **1280×800** or **640×400** PNG/JPEG.

Generated by `listing/capture.spec.ts` into `listing/screenshots/`:

1. **`01-overview.png`** — Side panel showing the key list, Local/Session toggle, search, and a pretty-printed JSON value in the editor.
2. **`02-json-editor.png`** — CodeMirror editor focused, highlighting a complex nested object.
3. **`03-change-log.png`** — Recording panel with multiple captured changes, source badges visible.
4. **`04-inline-diff.png`** — Expanded change entry showing an inline JSON diff.
5. **`05-unified-diff.png`** — The same change toggled into unified diff mode.

Regenerate with:

```bash
bun run build
bunx playwright test --config=listing/playwright.capture.config.ts --headed
```

## Promotional video

Chrome Web Store videos must be hosted on YouTube. The raw recording is generated by the same capture script and written to `listing/video/demo.webm`. Upload it to YouTube (unlisted is fine) and paste the URL into the listing's **Promotional video** field.

The recording walks through: open panel → browse keys → edit JSON → save → observe change log → expand diff → toggle unified view. About 25 seconds, no narration.

---

## Category, tags, and search keywords

- **Category:** Developer Tools
- **Suggested search terms:** localStorage, sessionStorage, storage editor, JSON editor, web storage, devtools, debugger

## Support and links

- **Homepage:** https://github.com/pete-the-pete/local-storage-inspector
- **Support:** https://github.com/pete-the-pete/local-storage-inspector/issues
- **Privacy policy:** https://pete-the-pete.github.io/local-storage-inspector/privacy-policy

## Single purpose description

Local Storage Inspector exists to let developers inspect and edit a page's `localStorage` and `sessionStorage` from a side panel, with a proper JSON editor and a live change log.

## Permission justifications

- **`activeTab`** — Required to read and write the current tab's `localStorage` and `sessionStorage` when the user clicks the extension icon. No access until the user invokes the extension.
- **`scripting`** — Required to inject the content script that relays storage mutations to the side panel. Injection is on-demand, per-tab.
- **`sidePanel`** — The extension's entire UI is a side panel.
- **`optional_host_permissions: <all_urls>`** — Optional, requested per-site only when the user wants the change log to keep recording after a page reload. Users can decline and the extension still works for one-off inspection via `activeTab`.
- **No remote code.** All JavaScript ships in the extension package.
